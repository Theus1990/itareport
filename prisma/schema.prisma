// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema
datasource db {
  provider = "postgresql"
  url = env("POSTGRES_PRISMA_URL") // uses connection pooling
  directUrl = env("POSTGRES_URL_NON_POOLING") // uses a direct connection
  shadowDatabaseUrl = env("POSTGRES_URL_NON_POOLING") // used for migrations
}

generator client {
  provider = "prisma-client-js"
}

model Usuario {
  id_user   String      @id @default(uuid()) @db.Uuid
  createdAt DateTime    @default(now())
  email     String      @unique
  cpf       String      @db.VarChar(15)
  name      String
  password  String
  denuncias Denuncia[]
  
}

model Denuncia {
  id_report   String    @id @default(uuid()) @db.Uuid
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  validated   Boolean   @default(false)
  title       String    @db.VarChar(255)
  content     String    @db.Text
  adress      String    @db.VarChar(255)
  userId      String    @db.Uuid
  user        Usuario   @relation(fields: [userId], references: [id_user])
}
  //categoriaId String    @db.Uuid
  //enderecoId  String    @db.Uuid
  //Categoria   Categoria @relation(fields: [categoriaId], references: [id_report])
  //Endereco    Endereco  @relation(fields: [enderecoId], references: [id_endereco])



//model Categoria {
//  id_report       String     @id @default(uuid()) @db.Uuid
//  nome_categoria  String     @db.VarChar(50)
//  denuncias       Denuncia[]
//}
//
//model Endereco {
//  id_endereco  String     @id @default(uuid()) @db.Uuid
//  rua          String     @db.VarChar(100)
//  numero       String     @db.VarChar(5)
//  bairro       String     @db.VarChar(20)
//  cidade       String     @db.VarChar(30)
//  denuncias    Denuncia[]
//}

